# Maps sms-backup-restore-exporter-csv columns → vault NDJSON fields.
# Left = CSV header; right = vault JSON field.
# Unmapped CSV columns: `# name = ""` (commented; do not enable with a real field).

source_id = "sms-backup-restore"
export_source = "sms-backup-restore"
exporter_version = "0.1.0"
schema = "vault"
default_service = "SMS"

[columns]
chat_identifier = "chat_identifier"
conversation_type = "conversation_type"
group_title = "group_title"
guid = "guid"
timestamp = "timestamp"
timestamp_utc = "timestamp_utc"
# timestamp_display = ""   # locale display string; use timestamp / timestamp_utc
direction = "direction"
service = "service"
sender_handle = "sender"
sender_display_name = "sender_display_name"
subject = "subject"
text = "text"
attachments_json = "attachments"
# export_source = ""       # ingest source id covers provenance
# message_kind = ""        # exporter-only
# date_ms = ""             # exporter-only
# contact_name = ""        # folded via sender_display_name / participants
# android_type = ""        # exporter-only
# xml_fields_json = ""     # exporter-only debug blob

[transforms]
direction_to_is_from_me = true
attachments_json_parse = true

[required]
fields = ["chat_identifier", "timestamp", "direction"]
require_text_or_attachments = true
