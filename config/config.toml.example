# Copy to config.toml and edit for your machine:
#   cp config/config.toml.example config/config.toml
#   cp config/contacts.csv.example config/contacts.csv
#   cp config/blacklist.csv.example config/blacklist.csv
#
# Real config/contacts/blacklist files are gitignored.
# contacts.csv: phone numbers only (semicolon-separated). Set exclude=true to hide from
# Contacts/groups (All still lists them). iMessage emails can be attached in the UI and
# live only in the DB — they are never written to this CSV.

[owner]
display_name = "Your Name"
phone_e164 = "+15555550100"
emails = ["you@example.com"]

[paths]
db = "data/vault.db"
data_dir = "data"
assets_dir = "assets"
assets_converted_dir = "assets_converted"
contacts_csv = "config/contacts.csv"
blacklist_csv = "config/blacklist.csv"

[[sources]]
id = "imessage"
export_dir = "staging/imessage"
# Optional: raw input for `ingest` (omit --from when set)
# source_dir = "/path/to/iphone_backup"

[[sources]]
id = "sms-backup-plus"
export_dir = "staging/sms-backup-plus-eml"
# One tree:
# source_dir = "/path/to/eml-tree"
# Or several trees (merged at export; sms-backup-plus only):
# source_dirs = ["/path/to/eml-a", "/path/to/eml-b"]

[[sources]]
id = "go-sms-pro"
export_dir = "staging/go-sms-pro"
# source_dir = "/path/to/gosms-export"

[[sources]]
id = "sms-backup-restore"
export_dir = "staging/sms-backup-restore"
# source_dir = "/path/to/sms-xml-or-dir"
